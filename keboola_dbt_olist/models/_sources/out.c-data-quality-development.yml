version: 2
sources:
    - name: out.c-data-quality-development
      freshness:
        warn_after:
            count: 1
            period: day
      database: '{{ env_var("DBT_KBC_KEBOOLA_DBT_OLIST_DATABASE") }}'
      schema: out.c-data-quality-development
      loaded_at_field: '"_timestamp"'
      tables:
        - name: dq_input
          quoting:
            database: true
            schema: true
            identifier: true
          columns: []
