version: 2
sources:
    - name: in.WS_ELEMENTARY
      freshness:
        warn_after:
            count: 1
            period: day
      database: '{{ env_var("DBT_KBC_KEBOOLA_DBT_OLIST_DATABASE") }}'
      schema: in.WS_ELEMENTARY
      loaded_at_field: '"_timestamp"'
      tables:
        - name: test_export
          quoting:
            database: true
            schema: true
            identifier: true
          columns: []
